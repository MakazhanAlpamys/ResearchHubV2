# ResearchHubV2 — Frontend

Flutter-приложение. Подробная инструкция в корневом [README.md](../README.md).

```bash
flutter pub get
flutter run
```

Перед запуском заполни `lib/core/constants/api_constants.dart`.
